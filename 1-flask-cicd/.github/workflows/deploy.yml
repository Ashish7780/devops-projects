name: CI/CD Flask App to EC2

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up SSH
      run: echo "Configure SSH manually for EC2 deployment. No credentials included."

    - name: Deployment
      run: echo "Deployment steps should be handled securely outside of code."
